#include "holberton."

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */

void print_alphabet(void)
{
	char vr;

	for (vr = 'a'; vr <= 'z'; vr++)
	{
		_putchar(vr);
	}

	_putchar('\n');

	return (0);
}
