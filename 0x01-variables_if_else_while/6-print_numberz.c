#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{

	int num;

	for (num = 0; x < 10; x++)
	{
		int digit = 48 + x;

		putchar(digit);
	}
		putchar('\n');

		return (0);
}
