#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
	char pls;

	for (pls = 'a'; pls <= 'z'; pls++)
		putchar(pls);
		putchar('\n');

		return (0);
}
