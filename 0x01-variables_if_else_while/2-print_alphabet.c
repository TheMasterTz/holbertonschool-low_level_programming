#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
	char cont;

	for (cont = 'a'; cont <= 'z'; cont++)

		putchar(cont);
		putchar('\n');

		return (0);
}
